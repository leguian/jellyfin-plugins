{
  "name": "customized-home-tests",
  "private": true,
  "description": "Browser tests of the Customized Home client script against a mocked jellyfin-web home page.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "typecheck": "tsc --noEmit",
    "screenshots": "playwright test -c playwright.screenshots.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^26.6.1",
    "typescript": "^5.6.0"
  },
  "type": "module"
}
